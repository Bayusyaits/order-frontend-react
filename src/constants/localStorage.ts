/**
 * All localStorage keys used by the application
 */
export enum Storage {
  SMD_TOKEN = 'smd_token',
  WMS_TOKEN = 'wms_token',
  WAREHOUSES = 'wh',
  WAREHOUSES_CACHED_ON = 'wco',
  SELECTED_WAREHOUSE = 'swh',
}
